<div data-popover-target="card" class="popover-reaction-card">
  <div class="flex-col">
    <% @reactions.each do |reaction| %>
      <div>
        <%= reaction.organization_user.display_name %>
        <%= time_ago_in_words reaction.created_at %>
      </div>
    <% end %>
  </div>
</div>