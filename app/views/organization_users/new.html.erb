<section class="form-section">
  <h2 class="form-header">Create user</h2>

  <%= form_for(@organization_user, html: { class: "default-form" }) do |f|
  %>
    <%= render "devise/shared/error_messages", resource: @organization_user %>

    <%= f.hidden_field :organization_id, value: current_organization.id %>

    <%= f.fields_for :user do |user_form| %>
      <div class="form-field">
        <%= user_form.label :email %>
        <%= user_form.email_field :email, class: "form-input"  %>
      </div>

      <div class="form-field">
        <%= user_form.label :first_name %>
        <%= user_form.text_field :first_name, class: "form-input"  %>
      </div>

      <div class="form-field">
        <%= user_form.label :last_name %>
        <%= user_form.text_field :last_name, class: "form-input"  %>
      </div>

      <div class="form-field">
        <%= user_form.label :password %>
        <%= user_form.password_field :password, class: "form-input"  %>
      </div>

      <div class="form-field">
        <%= user_form.label :password_confirmation %>
        <%= user_form.password_field :password_confirmation, autocomplete: "new-password", class: "form-input"  %>
      </div>
    <% end %>

    <div class="form-footer">
      <%= link_to "Cancel", organization_path(current_organization), class: "secondary-button" %>
      <%= f.submit "Create", class: "form-submit" %>
    </div>
  <% end %>
</section>
