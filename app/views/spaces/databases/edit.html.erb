<% content_for :head do %>
  <meta name="turbo-cache-control" content="no-cache">
<% end %>

<div class="h-full grid grid-cols-[minmax(10px,300px)_minmax(10px,_3fr)]">
  <%= render 'application/left_side_bar_for_database' %>

  <article class="bg-white flex-col overflow-auto">
    <%= react_component "EditTablePanel", {table: @table.attributes.merge(data: @table.data_to_json), space: @space} %>
  </article>
</div>
