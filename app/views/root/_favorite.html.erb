<div id="<%= dom_id(object) %>">

  <div class="p-2">
    <div class="text-base font-semibold">
      <%= link_to object.title, polymorphic_path([object.space, object]) %>
    </div>
    <div class="font-normal text-gray-500">Last updated <%= time_ago_in_words object.updated_at %></div>
  </div>
</div>