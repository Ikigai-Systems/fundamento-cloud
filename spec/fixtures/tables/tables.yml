_fixture:
  model_class: Table

users:
  name: users
  organization: :is
  space: :is_default
  parent: is_default (Space)

projects:
  name: projects
  organization: :is
  space: :is_default
  parent: is_default (Space)

orders:
  name: "Orders"
  organization: :is
  space: :is_default
  parent: is_default (Space)