# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html
<% _fixture_sync = Base64.encode64(BlocknoteConverterService.blocks_to_yjs(BlocknoteConverterService.markdown_to_blocks("Fixture content"))) %>

_fixture:
  model_class: Document

one:
  id: one
  title: One
  organization_id: "is"
  space_id: "is_default"
  sync: !!binary "<%= _fixture_sync %>"

two:
  id: two
  title: Two
  organization_id: "is"
  space_id: "is_default"
  sync: !!binary "<%= _fixture_sync %>"
